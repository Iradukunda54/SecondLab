package interfaces;

/**
 * Completable interface.
 * Any entity that can be marked as completed must implement this interface.
 * Demonstrates: Interface usage in OOP.
 */
public interface Completable {
    /**
     * Returns true if the implementing entity is in a completed state.
     */
    boolean isCompleted();
}
